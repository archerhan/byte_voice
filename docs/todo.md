当前版本离线功能基本完备，打磨一下细节。

### Bug：

- [x] 录音时、暂停时播放进度条需要隐藏
- [x] 录音时点击暂停，录音按钮应该变成停止，即在暂停状态下也能立即停止
- [x] 录音结束后，内容区域空白
- [x] ASR 状态有错误，转写中的应该是转写错误

### 功能：

- [x] ASR 需要加一个导出原始音频的按钮、与其他导出项在一起
- [x] 录音时需要增加一个声波图
- [x] 标题可以右键重命名
- [x] 本地存储的文件夹命名
  - [x] models：存各种模型、manifest等

  - [x] data: 存 db、配置文件等

  - [x] audios：
    - [x] asr_src: asr 的“原始”音频
      - [x] 命名为 asr_uuid.wav
      - [x] 原始格式暂不保留，统一 wav 即可
      - [x] 导出时查一下 title 的值，命名为 title.wav

    - [x] tts_gen: tts 生成的音频
      - [x] 本地保存时命名为 tts_uuid.wav

      - [x] 导出时需要带上 title，即 title.wav
- [x] 右上角模型可用状态需要分别显示
  - [x] 不要显示具体的模型叫什么
  - [x] 只需显示 ASR 引擎、TTS 引擎 即可
- [x] ASR加一个重新转写的按钮
- [x] 去掉ASR 中的搜索框。
- [x] ASR 导出一栏中新加一个“重新识别”的按钮，点击后清空内容区域，重新识别语音
- [x] TTS 应该加一个新建按钮，否则就是在其他文档上修改
- [x] TTS 如果选中了已经合成完成的任务，合成语音按钮文案应该为重新合成
- [x] 前期需要限制音频长度
  - [x] 有爆内存风险，需要测试
  - [x] 后面考虑切割原始音频一段一段的读取音频
- [x] TTS 也要限制文字长度
  - [x] 需要进一步测试
  - [x] 防止等待时间过长导致用户以为卡死
- [ ] ~~TTS 也要加一个进度显示~~
- [x] 语音模型的下载
  - [x] 需要配置在阿里云 oss，测试一下 cloudflare 的 R2国内是否流畅

  - [x] 定义一个模型下载的 `models_manifest.json` 文件


### UI 改善：

- [ ] 录音的按钮需要改变一下样式
- [ ] 主题样式改成白色
- [ ] 弹窗太 low，仿照 vscode 弹窗的UI 重写
- [ ] 设置页面增加需要用户配置的设置项
